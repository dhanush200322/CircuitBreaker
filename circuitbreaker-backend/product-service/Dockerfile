FROM eclipse-temurin:17-jre-alpine
WORKDIR /app

# Copy the executable JAR generated by the existing Maven build
# Using a wildcard avoids hard-coding the version number
COPY target/*.jar app.jar

# Product Service runs on 8081
EXPOSE 8081

ENTRYPOINT ["java", "-jar", "app.jar"]
